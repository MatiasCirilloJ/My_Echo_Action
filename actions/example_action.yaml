---
name: "example_action"
runner_type: "python-script"
description: "Print message to standard output."
enabled: true
entry_point: "example_action.py"
parameters:
    host:
        type: "string"
        description: "Host."
        required: true
        position: 0
